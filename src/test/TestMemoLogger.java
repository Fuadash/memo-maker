package test;
import main.classes.FolderManager;
import main.classes.MemoConfig;
import org.junit.jupiter.api.Test;

import java.io.IOException;
import java.nio.file.Files;
import java.nio.file.Path;

public class TestMemoLogger {

    /*TODO: Test for making the memo file itself, whether it adds the text to the file, whether it adds text to an
        already existing file, whether it makes the memo when the folder already exists, seems good
    */
    @Test
    void testLogMemo() {

    }
}
